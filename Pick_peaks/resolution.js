function pickPeaks(arr){
    //pseudo-code
    //parcourir le tableau pour trouver le premier pic :
        // - comparer i avec i+1
        // - stocker valeur+ dans une temp
        // - stocker position pic
        // - si val[i] === val[i++] alors stocker que val[i]
        // - si val[0] > val[i] ne ps stocker
        // - si val[arr.length] > val[i] ne pas stocker
    // definir un creux pour trouver prochain pic:
        // - comparer i avec i ++
        // - stocker valeur plus basse
        // - stocker position basse
        // - si meme valeur entre pos[i] et pos[i++] stocker pos[i++]
        // - si val[i] < val [j] alors nouveau creux à pos[j] -> trouver nouveau pic

    arr.forEach(num => {
        
    });

    //  return {pos:[],peaks:[]}
}